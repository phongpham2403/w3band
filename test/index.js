function Print(M, N) {
    while (M > N) {
        if (M < N) {
            M = N - M
        }

        if (M > N) {
            M = M - N
        }

        if (M = N) {
            return M
        }
        return M
    } 
    
}

console.log(Print(100, 85));
